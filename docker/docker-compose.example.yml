# Example Docker Compose for blender-mcp
#
# This file lives in docker/. Copy it to docker-compose.yml and create a .env
# from docker/.env.example.
#
# Networking: this container only needs an outbound connection to the
# Blender addon's socket (BLENDER_HOST:BLENDER_PORT) on UR1 — it does not
# need to share a network with Blender's container. Published on all
# interfaces (LAN-reachable) since Claude clients connect from other
# machines on the LAN — unlike our loopback-only deployments (e.g.
# pihole-mcp), there is currently NO auth in front of this endpoint, and
# one of its tools (execute_blender_code) runs arbitrary Python inside
# Blender. Acceptable on a trusted home LAN; do not expose this port
# beyond the LAN without adding auth first.

services:
  blender-mcp:
    image: ghcr.io/rhamblen/blender-mcp:latest
    # To build locally instead of pulling the prebuilt image. The build
    # context is the repo root (one level up), where pyproject.toml and
    # src/ live; the Dockerfile stays under docker/:
    # build:
    #   context: ..
    #   dockerfile: docker/Dockerfile
    container_name: blender-mcp
    environment:
      MCP_TRANSPORT: streamable-http
      MCP_HOST: 0.0.0.0
      MCP_PORT: "8000"
      BLENDER_HOST: ${BLENDER_HOST}
      BLENDER_PORT: ${BLENDER_PORT:-9876}
      BLENDER_MCP_DISABLE_TELEMETRY: "true"
      TZ: ${TZ:-UTC}
    ports:
      - "${MCP_PUBLISH_PORT:-8877}:8000"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid
    mem_limit: 256m
    cpus: 0.3
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
